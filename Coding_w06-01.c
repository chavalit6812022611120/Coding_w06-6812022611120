#include <stdio.h> 

int main() { 

    // กำหนดค่าเริ่มต้นของตัวแปร
    int a = 5;       // เราสร้างกล่องชื่อ 'a' แล้วใส่เลข 5 เข้าไป กล่องนี้ใส่ได้แต่ 'เลขเต็มๆ' นะ (int ย่อมาจาก integer)
    int b = 17;      // เหมือนเดิมเลย สร้างกล่องชื่อ 'b' ใส่เลข 17 เข้าไป เป็นเลขเต็มๆ เหมือนกัน
    double c = 8.5;  // คราวนี้สร้างกล่องชื่อ 'c' ใส่เลข 8.5 เข้าไป กล่องนี้พิเศษหน่อย ใส่ 'เลขทศนิยม' ได้ด้วย (double คือเลขทศนิยมที่ละเอียดหน่อย)
    double d = 4.0;  // สร้างกล่องชื่อ 'd' ใส่เลข 4.0 เข้าไป กล่องนี้ก็ใส่เลขทศนิยมได้เหมือนกัน

    // บรรทัดที่ 1: แสดงค่าเริ่มต้นของตัวแปรทั้งหมด
    printf("บรรทัดที่ 1: ค่าเริ่มต้นของตัวแปร:\n"); // เราบอกให้คอมพิวเตอร์ 'พิมพ์ข้อความ' นี้ออกมา
    printf("   a = %d\n", a); // พิมพ์ "   a = " แล้วตามด้วย 'ค่าในกล่อง a' (%d คือบอกว่าให้เอาเลขเต็มๆ มาแสดงนะ) แล้วก็ขึ้นบรรทัดใหม่ (\n)
    printf("   b = %d\n", b); // ทำเหมือนเดิมกับกล่อง b
    printf("   c = %.1f\n", c); // ทำกับกล่อง c แต่คราวนี้บอกว่า ให้เอาเลขทศนิยมมาแสดงนะ (%f) แล้วเอาแค่ทศนิยม 1 ตำแหน่งก็พอ (%.1f)
    printf("   d = %.1f\n", d); // ทำกับกล่อง d เหมือนกัน
    printf("------------------------------\n"); // พิมพ์เส้นคั่น เพื่อให้ดูเป็นระเบียบ

    // บรรทัดที่ 2: ผลการคำนวณของ d + a
    double result_d_plus_a = d + a; // เราสร้างกล่องใหม่ชื่อ 'result_d_plus_a' กล่องนี้เป็นแบบใส่เลขทศนิยมได้
                                     // แล้วก็เอา 'ค่าในกล่อง d' มาบวกกับ 'ค่าในกล่อง a' ผลลัพธ์ที่ได้ใส่ไปในกล่องใหม่นี้
    printf("บรรทัดที่ 2: ผลการคำนวณของ d + a = %.1f\n", result_d_plus_a); // พิมพ์ผลบวกออกมา
    printf("------------------------------\n");

    // บรรทัดที่ 3: ผลการคำนวณของ a - b
    int result_a_minus_b = a - b; // สร้างกล่องใหม่ชื่อ 'result_a_minus_b' เป็นแบบเลขเต็มๆ
                                  // เอา 'ค่าในกล่อง a' มาลบด้วย 'ค่าในกล่อง b' แล้วเก็บผลลัพธ์
    printf("บรรทัดที่ 3: ผลการคำนวณของ a - b = %d\n", result_a_minus_b); // พิมพ์ผลลบออกมา
    printf("------------------------------\n");

    // บรรทัดที่ 4: ผลการคำนวณของ c * d
    double result_c_times_d = c * d; // สร้างกล่องใหม่ชื่อ 'result_c_times_d' เป็นแบบทศนิยม
                                     // เอา 'ค่าในกล่อง c' มาคูณกับ 'ค่าในกล่อง d' แล้วเก็บผลลัพธ์
    printf("บรรทัดที่ 4: ผลการคำนวณของ c * d = %.1f\n", result_c_times_d); // พิมพ์ผลคูณออกมา
    printf("------------------------------\n");

    // บรรทัดที่ 5: ผลการคำนวณของ a * c
    double result_a_times_c = a * c; // สร้างกล่องใหม่ชื่อ 'result_a_times_c' เป็นแบบทศนิยม
                                     // เอา 'ค่าในกล่อง a' มาคูณกับ 'ค่าในกล่อง c' แล้วเก็บผลลัพธ์
    printf("บรรทัดที่ 5: ผลการคำนวณของ a * c = %.1f\n", result_a_times_c); // พิมพ์ผลคูณออกมา
    printf("------------------------------\n");

    // บรรทัดที่ 6: ผลการคำนวณของ c / d
    double result_c_div_d = c / d; // สร้างกล่องใหม่ชื่อ 'result_c_div_d' เป็นแบบทศนิยม
                                   // เอา 'ค่าในกล่อง c' มาหารกับ 'ค่าในกล่อง d' แล้วเก็บผลลัพธ์
    printf("บรรทัดที่ 6: ผลการคำนวณของ c / d = %.3f\n", result_c_div_d); // พิมพ์ผลหารออกมา โดยเอาทศนิยม 3 ตำแหน่ง
    printf("------------------------------\n");

    // บรรทัดที่ 7: ผลการคำนวณของ b / c
    double result_b_div_c = (double)b / c; // สร้างกล่องใหม่ชื่อ 'result_b_div_c' เป็นแบบทศนิยม
                                          // ตรงนี้สำคัญ: กล่อง 'b' เป็นเลขเต็ม แต่เราอยากให้หารออกมาเป็นทศนิยม
                                          // เราเลยบอกว่า "เฮ้! มอง 'b' ให้เป็นทศนิยมก่อนนะ (double)b"
                                          // แล้วค่อยเอาไปหารกับ 'c' ที่เป็นทศนิยมอยู่แล้ว
    printf("บรรทัดที่ 7: ผลการคำนวณของ b / c = %.3f\n", result_b_div_c); // พิมพ์ผลหารออกมา
    printf("------------------------------\n");

    // บรรทัดที่ 8: ผลการคำนวณของ a % b (Modulo - การหารเอาเศษ)
    // หมายเหตุ: เครื่องหมาย % นี้ ในภาษา C มันถูกสร้างมาให้ใช้กับ 'เลขเต็มๆ' เท่านั้นนะ
    int result_a_mod_b = a % b; // สร้างกล่องใหม่ชื่อ 'result_a_mod_b' เป็นแบบเลขเต็ม
                                // เอา 'ค่าในกล่อง a' (5) มาหารด้วย 'ค่าในกล่อง b' (17) แล้วเอา 'เศษที่เหลือ'
                                // 5 หาร 17 ได้ 0 ครั้ง เหลือเศษ 5 ดังนั้นผลลัพธ์คือ 5
    printf("บรรทัดที่ 8: ผลการคำนวณของ a %% b (หารเอาเศษ) = %d\n", result_a_mod_b); // พิมพ์เศษออกมา (%d)
    printf("------------------------------\n");

    // บรรทัดที่ 9: ผลการคำนวณของ c % a
    // ***** สาเหตุที่หาค่าไม่ได้: *****
    // เหมือนที่บอกไปแล้วว่าเครื่องหมาย % มันใช้กับ 'เลขเต็มๆ' เท่านั้น
    // แต่ 'c' (8.5) มันเป็น 'เลขทศนิยม' นี่สิ!
    // พอเราเอา 'เลขทศนิยม' มาใช้กับเครื่องหมาย % คอมพิวเตอร์ก็งงสิครับ "ฉันทำแบบนี้ไม่ได้นะ!"
    // มันเลยทำงานต่อไปไม่ได้ (หรือเกิดข้อผิดพลาดตอนที่เราสั่งให้คอมพิวเตอร์แปลงโค้ดของเรา)
    // ถ้าอยากหาเศษของเลขทศนิยมจริงๆ ต้องใช้ตัวช่วยอื่นชื่อ 'fmod()' ที่อยู่ในสมุดจดอีกเล่มชื่อ <math.h> ครับ
    printf("บรรทัดที่ 9: (ไม่สามารถหาค่าได้ด้วย '%%' เพราะ c เป็นทศนิยม)\n"); // เลยต้องพิมพ์ข้อความบอกว่าทำไม่ได้นะ
    printf("------------------------------\n");

    // บรรทัดที่ 10: ผลการคำนวณของ c % d
    // ***** สาเหตุที่หาค่าไม่ได้: *****
    // ก็เหมือนกับบรรทัดที่ 9 เลยครับ
    // ทั้ง 'c' (8.5) และ 'd' (4.0) ต่างก็เป็น 'เลขทศนิยม' ทั้งคู่
    // พอเอาเลขทศนิยมมาใช้กับเครื่องหมาย % คอมพิวเตอร์ก็ส่ายหน้าอีกแล้วครับ "แบบนี้ก็ไม่ได้!"
    // สรุปคือเครื่องหมาย % ใช้กับเลขเต็มเท่านั้น ถ้าอยากได้เศษจากเลขทศนิยม ต้องไปพึ่ง fmod() ครับ
    printf("บรรทัดที่ 10: (ไม่สามารถหาค่าได้ด้วย '%%' เพราะ c และ d เป็นทศนิยม)\n"); // พิมพ์ข้อความแจ้งเหมือนเดิม
    printf("------------------------------\n");

    return 0; // บรรทัดสุดท้ายนี้ เหมือนบอกว่า "เล่านิทานจบแล้วนะ ไม่มีอะไรติดค้างแล้ว" โปรแกรมก็จบการทำงานแบบไม่มีปัญหา
}